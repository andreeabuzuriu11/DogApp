<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <import type="android.view.View" />

        <variable
            name="viewModel"
            type="com.buzuriu.dogapp.viewModels.SelectStateViewModel" />

        <variable
            name="stateObj"
            type="com.buzuriu.dogapp.models.StateObj" />

    </data>

    <RelativeLayout
        android:id="@+id/state_cell_layout"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:padding="10dp">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerVertical="true"
            android:layout_marginLeft="@dimen/activity_horizontal_margin"
            android:text="@{stateObj.state.name}"
            android:textSize="@dimen/normal_size"
            tools:text="Romania" />

        <ImageView
            android:layout_width="50dp"
            android:layout_height="50dp"
            android:layout_alignParentEnd="true"
            android:src="@drawable/ic_baseline_check_24"
            android:visibility="@{stateObj.isSelected ? View.VISIBLE:View.GONE }" />


    </RelativeLayout>

</layout>